VITE_APP_URL=
VITE_EXTRA_URL=
VITE_API_PATHS=

# Локальные скрипты scripts/db/* (Node, не Vite). Не коммить реальные значения в .env
# Пример: mysql://user:password@host:3306/database_name
# Строка из SQLAlchemy: mysql+aiomysql://... — тоже подойдёт, драйвер нормализует префикс.
DATABASE_URL=